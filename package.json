{
  "name": "centra-manager",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm run dev --prefix frontend --",
    "vercel-dev": "vercel dev",
    "build": "npm install --prefix frontend && npm run build --prefix frontend && rm -rf public && mkdir -p public && cp -r frontend/dist/* public/"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.2",
    "jose": "^6.2.3",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3"
  }
}
